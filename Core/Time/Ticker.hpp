#ifndef TICKER_HPP_INCLUDED
#define TICKER_HPP_INCLUDED

/* A ticker tells when a certain amount of time went by
 * They can be linked to objects to update them every x in whatever way */

class Ticker
{
	protected:
		// Listeners
	public:
		// 
};

#endif