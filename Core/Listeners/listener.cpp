#include "Listener.hpp"

void Listener::onEvent(sf::Event const& event)
{
	if(event.type == sf::Event::MouseButtonPressed) // Mouse click
	{
		
	}
	else if(event.type == sf::Event::MouseButtonReleased) // Mouse released
	{
		
	}
	else if(event.type == sf::Event::MouseMoved) // Mouse movement
	{
		
	}
	else if(event.type == sf::Event::KeyPressed) // Key pressed
	{
		
	}
	else if(event.type == sf::Event::KeyReleased) // Key released
	{
		
	}
}